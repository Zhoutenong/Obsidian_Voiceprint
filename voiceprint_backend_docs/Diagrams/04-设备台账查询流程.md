# 设备台账查询流程时序图

## 流程说明

设备台账模块展示变电站设备的层次结构、基础信息、运行趋势、异常分布、巡检日志等。用户可以通过设备树选择设备，查看详细信息和下载相关音频。

> **实现说明**: 设备台账相关 API 均实现在 `VoiceprintPortalAppService` 类中，数据源为关系数据库（通过 SqlSugar ORM）。

## 时序图

```mermaid
sequenceDiagram
    autonumber
    participant User as 👤 用户
    participant Frontend as 🌐 前端应用
    participant API as 🔌 API 网关
    participant PortalService as 📦 门户服务<br/>(VoiceprintPortalAppService)
    participant Database as 💾 数据库
    participant FileStorage as 📁 文件存储

    Note over User,FileStorage: 页面初始化
    User->>Frontend: 打开设备台账页面
    Frontend->>API: GET /api/app/voiceprint/assets/tree
    API->>PortalService: 获取设备台账树
    PortalService->>Database: SELECT 设备层次结构
    Database-->>PortalService: 设备树数据
    PortalService-->>Frontend: 返回设备树
    Frontend->>User: 渲染左侧设备树

    Note over User,FileStorage: 查看设备基础信息
    User->>Frontend: 在设备树中选择设备
    Frontend->>Frontend: 更新 URL: ?monitoredObjectId=xxx
    Frontend->>API: GET /api/app/voiceprint/assets/{monitoredObjectId:guid}
    API->>PortalService: 查询设备基础信息
    PortalService->>Database: SELECT 设备详情
    Database-->>PortalService: 设备详细数据
    PortalService-->>Frontend: { name, type, location, status, ... }
    Frontend->>User: 显示设备基础信息卡片

    Note over User,FileStorage: 并行加载设备详细数据
    par 查询设备运行趋势
        Frontend->>API: GET /api/app/voiceprint/assets/{monitoredObjectId:guid}/trend<br/>?startTime=2026-05-01
        API->>PortalService: 查询设备运行趋势
        PortalService->>Database: SELECT 音频记录时序数据<br/>(VoiceprintDeviceAudioRecordEntity)
        Database-->>PortalService: 返回趋势数据
        PortalService-->>Frontend: [{ collectedAt, isNormal }]
        Frontend->>User: 渲染趋势图表
    and 查询设备异常分布
        Frontend->>API: GET /api/app/voiceprint/assets/{monitoredObjectId:guid}/anomaly-mix
        API->>PortalService: 查询设备异常分布
        PortalService->>Database: SELECT 异常记录并分组统计
        Database-->>PortalService: 异常分类统计
        PortalService-->>Frontend: [{ anomalyType, count, ratio }]
        Frontend->>User: 渲染异常分布饼图
    and 查询设备巡检日志
        Frontend->>API: GET /api/app/voiceprint/assets/{monitoredObjectId:guid}/logs<br/>?page=1&limit=50
        API->>PortalService: 查询巡检日志
        PortalService->>Database: SELECT 巡检记录
        Database-->>PortalService: 日志列表（分页）
        PortalService-->>Frontend: { items, total, page }
        Frontend->>User: 显示巡检日志列表
    end

    Note over User,FileStorage: 下载处理后的音频
    User->>Frontend: 选择时间范围下载音频
    User->>Frontend: 点击"批量下载"按钮
    Frontend->>API: GET /api/app/voiceprint/assets/{monitoredObjectId:guid}/processed-audios/download<br/>?startTime=xxx&endTime=xxx
    API->>PortalService: 按时间范围查询并打包音频文件
    PortalService->>FileStorage: 读取音频文件
    FileStorage-->>PortalService: 音频文件流
    PortalService->>PortalService: 打包成 ZIP
    PortalService-->>Frontend: 直接返回 ZIP 文件流 (FileStreamResult)
    Frontend->>User: 自动下载 ZIP 文件

    Note over User,FileStorage: 切换设备
    User->>Frontend: 在设备树中选择另一个设备
    Frontend->>Frontend: 重复上述查询流程
    Note right of Frontend: 使用 React Query 缓存<br/>避免重复请求相同数据
```

## 关键接口

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/api/app/voiceprint/assets/tree` | GET | 获取设备台账树 |
| `/api/app/voiceprint/assets/{monitoredObjectId:guid}` | GET | 设备基础信息 |
| `/api/app/voiceprint/assets/{monitoredObjectId:guid}/trend` | GET | 设备运行趋势（参数: startTime） |
| `/api/app/voiceprint/assets/{monitoredObjectId:guid}/anomaly-mix` | GET | 设备异常分布（无需时间参数） |
| `/api/app/voiceprint/assets/{monitoredObjectId:guid}/logs` | GET | 设备巡检日志（默认 pageSize=50） |
| `/api/app/voiceprint/assets/{monitoredObjectId:guid}/processed-audios/download` | GET | 下载处理后的音频（参数: startTime, endTime），直接返回 ZIP 文件流 |

## 请求/响应数据结构

### 趋势接口响应

```json
[{ "collectedAt": "DateTime", "isNormal": "bool" }]
```

### 异常分布接口响应

```json
[{ "anomalyType": "string", "count": "int", "ratio": "double" }]
```

> **注意**: 异常分布接口不接受时间参数，直接查询全部历史记录进行分组统计。

## 数据源说明

- **设备树、设备详情、异常分布、巡检日志**: 均通过 SqlSugar ORM 查询主关系数据库（SQLite/MySQL/SQL Server 等）
- **运行趋势**: 从 `VoiceprintDeviceAudioRecordEntity` 表查询，同样使用关系数据库
- **音频文件**: 从文件存储系统读取，打包 ZIP 后以 `FileStreamResult` 直接返回

## 前端使用位置

- `src/hooks/assets/useAssetTree.ts` - 设备树
- `src/hooks/assets/useDeviceInfo.ts` - 设备基础信息
- `src/hooks/ledger/useDeviceTrend.ts` - 设备运行趋势
- `src/hooks/ledger/useAnomalStat.ts` - 异常分布
- `src/hooks/ledger/useLogList.ts` - 巡检日志
- `src/hooks/ledger/useDownloadAudio.ts` - 音频下载

## 相关文档

- [VoiceprintPortalAppService](../Modules/ast-voiceprint/VoiceprintPortalAppService.md)
- [DeviceService](../Modules/ast-intellisub/DeviceService.md)
- [AssetsAPI](../API/Assets/AssetsAPI.md)
- [传感器数据管理服务](../Modules/ast-intellisubdata/传感器数据管理服务.md)
- [TDengine 集成](../Modules/ast-intellisubdata/TDengine集成.md)
