# 声纹分析流程时序图

## 流程说明

声纹分析模块包括：标准音频库管理、测试音频导入与识别、从音频生成报告等功能。用户可以上传测试音频进行声纹比对，并生成分析报告。

> **实现说明**: 标准音频库和测试音频导入通过 `VoiceprintPortalAppService` 实现，报告生成和文件上传通过 `VoiceprintAudioAppService` 实现。

## 时序图

```mermaid
sequenceDiagram
    autonumber
    participant User as 👤 用户
    participant Frontend as 🌐 前端应用
    participant API as 🔌 API 网关
    participant PortalService as 🎵 门户服务<br/>(VoiceprintPortalAppService)
    participant AudioService as 🎙️ 音频服务<br/>(VoiceprintAudioAppService)
    participant FileStorage as 📁 文件存储
    participant Database as 💾 数据库
    participant EdgeDevice as 📡 边缘设备

    Note over User,EdgeDevice: 查询标准音频库
    User->>Frontend: 打开声纹分析页面
    Frontend->>API: GET /api/app/voiceprint/standard-audios
    API->>PortalService: 查询标准音频库（返回全部数据）
    PortalService->>Database: SELECT 标准音频
    Database-->>PortalService: 标准音频列表
    PortalService-->>Frontend: 返回全部标准音频列表
    Frontend->>User: 显示标准音频列表

    Note over User,EdgeDevice: 上传测试音频进行识别
    User->>Frontend: 选择测试音频文件
    User->>Frontend: 点击"导入并识别"
    Frontend->>Frontend: 创建 FormData<br/>仅包含 { file }
    Frontend->>API: POST /api/app/voiceprint/test-audios/import<br/>Content-Type: multipart/form-data
    API->>PortalService: 处理音频导入（仅接收 IFormFile）
    PortalService->>FileStorage: 保存原始音频文件
    FileStorage-->>PortalService: 文件路径
    PortalService->>Database: 创建测试音频记录
    Database-->>PortalService: 记录创建成功（audioId）
    PortalService->>PortalService: 触发声纹识别

    alt 识别成功
        PortalService->>EdgeDevice: 发送音频进行识别
        EdgeDevice-->>PortalService: 返回识别结果<br/>{ similarity, matchedId, confidence }
        PortalService->>Database: 保存识别结果
        PortalService-->>API: { audioId, similarity, result }
        API-->>Frontend: HTTP 200 OK
        Frontend->>User: 显示识别结果
        Frontend->>User: 提示"识别成功，相似度 85%"
    else 识别失败
        PortalService-->>API: { error: "识别失败" }
        API-->>Frontend: HTTP 500 Error
        Frontend->>User: 显示错误提示
    end

    Note over User,EdgeDevice: 从音频生成报告
    User->>Frontend: 上传音频文件填写信息
    User->>Frontend: 点击"生成报告"
    Frontend->>API: POST /api/app/voiceprint/reports/generate-from-audio<br/>Content-Type: multipart/form-data<br/>{ diagnosisType, audioFile, audioFileName, resultFileName? }
    API->>AudioService: 接收 FormData 并生成报告
    AudioService->>AudioService: 生成 DOCX 报告
    Note right of AudioService: 填充报告模板<br/>插入音频信息<br/>添加图表
    AudioService->>FileStorage: 保存报告文件
    FileStorage-->>AudioService: 报告文件路径
    AudioService-->>API: { downloadUrl, filename }
    API-->>Frontend: HTTP 200 OK
    Frontend->>Frontend: 创建 <a> 标签
    Frontend->>User: 自动下载报告文件

    Note over User,EdgeDevice: 批量导入测试音频
    User->>Frontend: 选择多个测试音频
    User->>Frontend: 点击"批量导入"
    loop 遍历每个音频文件
        Frontend->>API: POST /api/app/voiceprint/test-audios/import<br/>Content-Type: multipart/form-data<br/>仅包含 { file }
        API->>PortalService: 处理单个音频（仅接收 IFormFile）
        PortalService->>PortalService: 触发识别
        PortalService-->>Frontend: 返回单个结果
    end
    Frontend->>User: 显示批量导入结果<br/>成功 5 个，失败 1 个
```

## 关键接口

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/api/app/voiceprint/standard-audios` | GET | 标准音频库列表（返回全部数据，无分页参数） |
| `/api/app/voiceprint/test-audios/import` | POST | 导入测试音频并触发识别（仅接收 file 参数） |
| `/api/app/voiceprint/reports/generate-from-audio` | POST | 从音频生成报告（multipart/form-data 格式） |

## 请求/响应数据结构

### 测试音频导入

- **请求格式**: `multipart/form-data`
- **参数**: 仅 `file` (IFormFile)，无 `deviceId` 或 `deviceName` 参数

### 从音频生成报告 (VoiceprintAudioReportGenerateInput)

- **请求格式**: `multipart/form-data`（通过 `[FromForm]` 绑定）
- **字段**:
  - `diagnosisType` (string) - 诊断类型
  - `audioFile` (IFormFile) - 音频文件
  - `audioFileName` (string) - 音频文件名
  - `resultFileName` (string?) - 可选，输出报告文件名

## 前端使用位置

> **注意**: 以下文件位于前端仓库 `intelli-substation-voiceprint-frontend`，不在当前后端仓库中。

- `src/hooks/voiceprint/useSampleList.tsx` - 标准音频列表
- `src/hooks/voiceprint/useAnalyze.tsx` - 音频识别
- `src/hooks/voiceprint/useGenerateReportFromAudio.tsx` - 从音频生成报告

## 相关文档

- [VoiceprintPortalAppService](../Modules/ast-voiceprint/VoiceprintPortalAppService.md)
- [VoiceprintAudioAppService](../Modules/ast-voiceprint/VoiceprintAudioAppService.md)
- [声纹分析流程](../Modules/ast-voiceprint/声纹分析流程.md)
- [声纹采集服务](../Modules/ast-voiceprint/声纹采集服务.md)
