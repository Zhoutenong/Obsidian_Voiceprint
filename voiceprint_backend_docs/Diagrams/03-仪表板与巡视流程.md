# 仪表板与巡视流程时序图

## 流程说明

仪表板是系统首页，展示巡视统计、设备状态、告警信息等核心数据。巡视流程包括：巡视记录查询、巡视结果查看、报告生成和导出等功能。

> **实现说明**: 仪表板和巡视相关 API 均实现在 `VoiceprintPortalAppService` 类中。

## 时序图

```mermaid
sequenceDiagram
    autonumber
    participant User as 👤 用户
    participant Frontend as 🌐 前端应用
    participant API as 🔌 API 网关
    participant PortalService as 📊 门户服务<br/>(VoiceprintPortalAppService)
    participant AudioService as 🎵 音频服务<br/>(VoiceprintAudioAppService)
    participant ReportService as 📄 报告服务
    participant Database as 💾 数据库
    participant FileStorage as 📁 文件存储

    Note over User,FileStorage: 用户打开首页
    User->>Frontend: 访问首页
    Frontend->>Frontend: 并行加载多个数据

    par 并行请求首页数据
        Frontend->>API: GET /api/app/voiceprint/dashboard/overview
        API->>PortalService: 查询巡视统计
        PortalService->>Database: 统计巡视天数和采集次数
        Database-->>PortalService: 统计结果
        PortalService-->>Frontend: { totalDays, collectionCount }
    and
        Frontend->>API: GET /api/app/voiceprint/dashboard/system-status
        API->>PortalService: 查询设备状态统计
        PortalService->>Database: 统计各状态设备数量
        Database-->>PortalService: 统计结果
        PortalService-->>Frontend: { normal, warning, fault }
    and
        Frontend->>API: GET /api/app/voiceprint/dashboard/alarms
        API->>PortalService: 查询最新告警
        PortalService->>Database: SELECT TOP 10 未处理告警
        Database-->>PortalService: 告警数据
        PortalService-->>Frontend: 告警列表
    end

    Frontend->>User: 渲染首页仪表板

    Note over User,FileStorage: 查询巡视记录
    User->>Frontend: 选择日期查看巡视记录
    Frontend->>API: GET /api/app/voiceprint/dashboard/records<br/>?date=2026-06-01
    API->>PortalService: 查询指定日期的巡视记录
    PortalService->>Database: SELECT 巡视记录
    Database-->>PortalService: 记录列表（包含 groupId）
    PortalService-->>Frontend: 返回记录列表
    Frontend->>User: 显示巡视记录卡片

    Note over User,FileStorage: 查看巡视详情
    User->>Frontend: 点击某条记录查看详情
    Frontend->>API: GET /api/app/voiceprint/dashboard/records/{groupId:guid}
    API->>PortalService: 查询巡视记录详情
    PortalService->>Database: SELECT 详细采集数据
    Database-->>PortalService: 详细数据（音频、告警、结果）
    PortalService-->>Frontend: 返回详情数据
    Frontend->>User: 显示巡视结果详情

    Note over User,FileStorage: 生成告警报告
    User->>Frontend: 在告警弹窗中点击"生成报告"
    Frontend->>API: POST /api/app/voiceprint/dashboard/generate-alarm-report<br/>{ devices: [{ diagnosisType, anomalyType, audioPath, deviceName, collectedAt }] }
    API->>PortalService: 触发报告生成任务
    PortalService->>PortalService: 查询告警详情
    PortalService->>PortalService: 查询相关音频
    PortalService->>PortalService: 生成 DOCX 报告
    PortalService->>FileStorage: 保存报告文件
    FileStorage-->>PortalService: 返回文件路径
    PortalService-->>Frontend: { downloadUrl, filename }
    Frontend->>User: 提示下载或自动下载

    Note over User,FileStorage: 导出智能巡视报表
    User->>Frontend: 点击"导出报告"按钮
    Frontend->>Frontend: 选择日期范围
    User->>Frontend: 确认导出
    Frontend->>API: POST /api/app/voiceprint/reports/export<br/>{ startTime, endTime, resultFileName? }
    API->>ReportService: 触发批量导出任务
    ReportService->>Database: 查询时间范围内的所有巡视数据
    Database-->>ReportService: 返回大量巡视记录
    ReportService->>ReportService: 生成汇总报告
    ReportService->>FileStorage: 保存汇总报告
    FileStorage-->>ReportService: 文件路径
    ReportService-->>Frontend: { downloadUrl }
    Frontend->>User: 下载报告文件

    Note over User,FileStorage: 切换算法模式
    User->>Frontend: 切换算法模式
    Frontend->>API: POST /api/app/voiceprint/algorithm/switch<br/>{ targetMode: 0 | 1 }
    API->>AudioService: 更新算法配置
    AudioService->>Database: UPDATE 配置表
    Database-->>AudioService: 更新成功
    AudioService-->>Frontend: HTTP 200 OK
    Frontend->>User: 显示模式切换成功
```

## 关键接口

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/api/app/voiceprint/dashboard/overview` | GET | 巡视统计（总天数、采集次数） |
| `/api/app/voiceprint/dashboard/records` | GET | 获取指定日期的巡视记录 |
| `/api/app/voiceprint/dashboard/records/{groupId:guid}` | GET | 巡视记录详情 |
| `/api/app/voiceprint/dashboard/system-status` | GET | 电子沙盘（设备状态统计） |
| `/api/app/voiceprint/dashboard/alarms` | GET | 首页报警信息 |
| `/api/app/voiceprint/dashboard/generate-alarm-report` | POST | 根据告警生成单次声纹分析报表 |
| `/api/app/voiceprint/reports/export` | POST | 导出智能巡视报表（按时间范围） |
| `/api/app/voiceprint/algorithm/switch` | POST | 切换算法模式 |

## 请求/响应数据结构

### 告警报告生成 (VoiceprintAlarmReportGenerateInput)

```json
{
  "devices": [{
    "diagnosisType": "string - 诊断类型",
    "anomalyType": "string - 异常类型",
    "audioPath": "string - 音频路径",
    "deviceName": "string - 设备名称",
    "collectedAt": "DateTime - 采集时间"
  }]
}
```

### 报表导出 (VoiceprintReportExportInput)

```json
{
  "startTime": "DateTime - 开始时间",
  "endTime": "DateTime - 结束时间",
  "resultFileName": "string? - 可选，输出文件名"
}
```

### 算法模式切换 (VoiceprintAlgorithmSwitchInput)

```json
{
  "targetMode": 0 | 1
}
```
- `targetMode = 0`: 恢复原模式/定时流程
- `targetMode = 1`: 手动增强模式/60秒采集

## 前端使用位置

- `src/hooks/patrol/usePatrolStatistic.ts` - 巡视统计
- `src/hooks/patrol/usePatrolRecord.ts` - 巡视记录
- `src/hooks/patrol/usePatrolResult.ts` - 巡视结果
- `src/hooks/ledger/useDeviceStatistics.ts` - 设备统计
- `src/hooks/alarm-record/useDashboardAlarmList.tsx` - 首页告警
- `src/hooks/report/useReport.tsx` - 报告导出
- `src/pages/system/_components/header/CollectIcon.tsx` - 算法切换

## 相关文档

- [VoiceprintPortalAppService](../Modules/ast-voiceprint/VoiceprintPortalAppService.md)
- [VoiceprintAudioAppService](../Modules/ast-voiceprint/VoiceprintAudioAppService.md)
- [声纹采集服务](../Modules/ast-voiceprint/声纹采集服务.md)
- [声纹分析流程](../Modules/ast-voiceprint/声纹分析流程.md)
